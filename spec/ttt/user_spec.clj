(ns ttt.user-spec
  (:require [speclj.core :refer :all]
            [ttt.user :refer :all]))

; (describe "get-spot"
;   ; FIXME test it loops infinitely if input is not valid
;   (it "returns integer corresponding to input")
;     (should= 0 (with-in-str "1" (get-spot))))
